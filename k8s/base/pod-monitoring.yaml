apiVersion: monitoring.googleapis.com/v1
kind: PodMonitoring
metadata:
  name: java-shop-pod-monitoring
  namespace: java-shop
spec:
  selector:
    matchLabels:
      app: java-shop-app
  endpoints:
    - port: 8080
      path: /actuator/prometheus
      interval: 30s